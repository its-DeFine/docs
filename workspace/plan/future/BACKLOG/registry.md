# Backlog Registry

Items captured from registry threads. Each entry is timestamped and numbered.

---

## BL-001 — `SHOWCASE_DISCORD_REVIEWER_USER_ID` context access warning (2026-03-24)

**Source:** VSCode Problems panel — `.github/workflows/project-showcase-sync.yml` Ln 57, Col 37
**Issue:** Warning that context access might be invalid for `SHOWCASE_DISCORD_REVIEWER_USER_ID`. The variable is referenced as `${{ vars.SHOWCASE_DISCORD_REVIEWER_USER_ID }}` but the linter flags it as potentially invalid.
**Status:** Logged — under investigation

---

